"use client";

import {useEffect,useMemo,useState} from "react";

const API="/backend";
export const DEFAULT_OPPORTUNITY_FORMULA={williams:60,ma100_proximity:30,ma100_slope:5,approach_velocity:5};
export type OpportunityCriteriaMap=Record<string,number>;
export type OpportunityHardFilter={field:string;operator:string;value:number};
export type ActiveOpportunityFormula={criteria:OpportunityCriteriaMap;filters:OpportunityHardFilter[];name:string;dirty:boolean;revision:number};
type SortKey="score"|"quality_adjusted_score"|"quality_confidence"|"williams"|"ma100_proximity"|"ma50_proximity"|"ma100_slope"|"approach_velocity"|"relative_volume"|"fundamental_quality"|"valuation_quality"|"liquidity"|"symbol";
type SortDir="asc"|"desc";

async function api(path:string,init?:RequestInit){
 const r=await fetch(`${API}${path}`,{credentials:"include",cache:"no-store",...init,headers:{"content-type":"application/json",...(init?.headers||{})}});
 const d=await r.json().catch(()=>({}));
 if(!r.ok)throw new Error(d?.detail||`HTTP ${r.status}`);
 return d;
}
const n=(v:any,d=1)=>v==null?"—":Number(v).toFixed(d);
const money=(v:any)=>v==null?"—":Number(v)>=1e9?`$${(Number(v)/1e9).toFixed(1)}B`:Number(v)>=1e6?`$${(Number(v)/1e6).toFixed(1)}M`:`$${Number(v).toLocaleString()}`;
function opportunityTone(x:any){
 const score=Number(x.score??50),wr=Number(x.raw_criteria?.williams??0),ma=Number(x.raw_criteria?.ma100_proximity??0);
 if(score>=80||((wr<=-80)&&(ma>=0&&ma<=5)))return "strength-strong";
 if(score>=68||wr<=-70)return "strength-positive";
 if(score<=38)return "strength-weak";
 if(score<=50)return "strength-negative";
 return "strength-neutral";
}

export default function OpportunityFormulaBuilderV4({onOpen,onFormulaState}:{onOpen:(symbol:string)=>void;onFormulaState?:(formula:ActiveOpportunityFormula)=>void}){
 const[catalog,setCatalog]=useState<any[]>([]),[filterCatalog,setFilterCatalog]=useState<any[]>([]),[criteria,setCriteria]=useState<OpportunityCriteriaMap>(DEFAULT_OPPORTUNITY_FORMULA),[filters,setFilters]=useState<OpportunityHardFilter[]>([]),[rows,setRows]=useState<any[]>([]),[meta,setMeta]=useState<any>(null),[presets,setPresets]=useState<any[]>([]);
 const[loading,setLoading]=useState(true),[error,setError]=useState(""),[addKey,setAddKey]=useState(""),[addFilterKey,setAddFilterKey]=useState(""),[saveName,setSaveName]=useState(""),[expanded,setExpanded]=useState<string|null>(null),[activePresetId,setActivePresetId]=useState<string|number|null>("default"),[dirty,setDirty]=useState(false),[revision,setRevision]=useState(0);
 const[sortKey,setSortKey]=useState<SortKey>("score"),[sortDir,setSortDir]=useState<SortDir>("desc");
 const activePreset=presets.find(p=>String(p.id)===String(activePresetId));
 const formulaName=activePreset?.name||"Custom Opportunity Formula";

 useEffect(()=>{onFormulaState?.({criteria,filters,name:formulaName,dirty,revision})},[criteria,filters,formulaName,dirty,revision,onFormulaState]);
 async function loadPresets(){const d=await api("/api/v1/opportunities/formulas");setPresets([d.default,...(d.saved||[])]);return d;}
 async function run(next:OpportunityCriteriaMap=criteria,nextFilters:OpportunityHardFilter[]=filters,nextSort:SortKey=sortKey,nextDir:SortDir=sortDir){
  setLoading(true);setError("");
  try{
   const q=new URLSearchParams({limit:"500",sort_by:nextSort,sort_dir:nextDir});
   const d=await api(`/api/v1/opportunities/index?${q.toString()}`,{method:"POST",body:JSON.stringify({criteria:next,filters:nextFilters})});
   setRows(d.rows||[]);setMeta(d);setRevision(x=>x+1);
  }catch(e:any){setError(e?.message||String(e))}finally{setLoading(false)}
 }
 useEffect(()=>{(async()=>{try{const[c,p]=await Promise.all([api("/api/v1/opportunities/criteria"),api("/api/v1/opportunities/formulas")]);setCatalog(c.criteria||[]);setFilterCatalog(c.filters||[]);setPresets([p.default,...(p.saved||[])]);const initial=c.default_formula?.criteria||DEFAULT_OPPORTUNITY_FORMULA,initialFilters=c.default_formula?.filters||[];setCriteria(initial);setFilters(initialFilters);await run(initial,initialFilters,"score","desc")}catch(e:any){setError(e?.message||String(e));setLoading(false)}})()},[]);

 const activeKeys=Object.keys(criteria),available=catalog.filter(c=>!activeKeys.includes(c.key));
 const effective=useMemo(()=>{const total=Object.values(criteria).reduce((a,b)=>a+Number(b||0),0);return Object.fromEntries(Object.entries(criteria).map(([k,v])=>[k,total?Number(v)/total*100:0]))},[criteria]);
 const labelFor=(key:string)=>catalog.find(c=>c.key===key)?.label||key;
 const infoFor=(key:string)=>catalog.find(c=>c.key===key)||{};
 const filterInfo=(field:string)=>filterCatalog.find(c=>c.key===field)||{label:field,operators:["<=",">="]};
 function markDirty(){setDirty(true)}
 function sort(key:SortKey){const nextDir:SortDir=sortKey===key?(sortDir==="desc"?"asc":"desc"):(key==="symbol"?"asc":"desc");setSortKey(key);setSortDir(nextDir);setExpanded(null);run(criteria,filters,key,nextDir)}
 function changeWeight(key:string,value:number){if(!Number.isFinite(value)||value<=0)return;markDirty();setCriteria(c=>({...c,[key]:value}))}
 function remove(key:string){if(activeKeys.length<=1)return;markDirty();const next={...criteria};delete next[key];setCriteria(next)}
 function add(){if(!addKey)return;markDirty();setCriteria(c=>({...c,[addKey]:10}));setAddKey("")}
 function addFilter(){const info=filterInfo(addFilterKey);if(!addFilterKey||!info)return;markDirty();setFilters(xs=>[...xs,{field:addFilterKey,operator:info.default_operator||info.operators?.[0]||"<=",value:Number(info.default_value??0)}]);setAddFilterKey("")}
 function updateFilter(i:number,patch:Partial<OpportunityHardFilter>){markDirty();setFilters(xs=>xs.map((x,j)=>j===i?{...x,...patch}:x))}
 function removeFilter(i:number){markDirty();setFilters(xs=>xs.filter((_,j)=>j!==i))}
 function usePreset(p:any){const next={...(p.criteria||DEFAULT_OPPORTUNITY_FORMULA)},nextFilters=[...(p.filters||[])];setActivePresetId(p.id);setSaveName(p.built_in?"":p.name);setDirty(false);setCriteria(next);setFilters(nextFilters);setExpanded(null);run(next,nextFilters,sortKey,sortDir)}
 async function save(){const name=saveName.trim();if(!name)return;setError("");try{const created=await api("/api/v1/opportunities/formulas",{method:"POST",body:JSON.stringify({name,criteria,filters})});setActivePresetId(created.id);setSaveName(created.name);setDirty(false);await loadPresets()}catch(e:any){setError(e?.message||String(e))}}
 async function updatePreset(){if(!activePreset||activePreset.built_in)return;const name=saveName.trim()||activePreset.name;setError("");try{const updated=await api(`/api/v1/opportunities/formulas/${activePreset.id}`,{method:"PUT",body:JSON.stringify({name,criteria,filters})});setSaveName(updated.name);setActivePresetId(updated.id);setDirty(false);await loadPresets()}catch(e:any){setError(e?.message||String(e))}}
 async function duplicatePreset(){const base=(saveName.trim()||activePreset?.name||"Opportunity Formula").replace(/\s+Copy$/i,"");setError("");try{const created=await api("/api/v1/opportunities/formulas",{method:"POST",body:JSON.stringify({name:`${base} Copy`,criteria,filters})});setSaveName(created.name);setActivePresetId(created.id);setDirty(false);await loadPresets()}catch(e:any){setError(e?.message||String(e))}}
 async function del(p:any){if(p.built_in)return;setError("");try{await api(`/api/v1/opportunities/formulas/${p.id}`,{method:"DELETE"});if(String(activePresetId)===String(p.id)){setActivePresetId("default");setSaveName("");setDirty(false)}await loadPresets()}catch(e:any){setError(e?.message||String(e))}}

 return <div className="formula-builder-v4">
  <div className="formula-toolbar"><div><span className="label">Active Opportunity formula</span><h2>{formulaName}{dirty?" · Modified":""}</h2><p className="muted">{meta?.formula?.label||"Weights are normalized automatically."} Index scores rank relative setup quality; they are not return probabilities.</p>{meta?.formula?.coverage_warning&&<p className="muted"><b>Coverage:</b> {meta.formula.coverage_warning}</p>}</div><div className="formula-actions"><select aria-label="Add Opportunity criterion" value={addKey} onChange={e=>setAddKey(e.target.value)}><option value="">Add Criteria…</option>{available.map(c=><option key={c.key} value={c.key}>{c.label} · {c.coverage_class==="cached_fundamentals_only"?"cached fundamentals":`${c.min_sessions||"?"} sessions`}</option>)}</select><button onClick={add} disabled={!addKey}>Add</button><button onClick={()=>run()} disabled={loading}>{loading?"Searching…":"Search"}</button></div></div>
  <div className="formula-criteria" aria-label="Active formula criteria">{activeKeys.map(key=>{const info=infoFor(key);return <div className="formula-criterion" key={key}><div><b>{labelFor(key)}</b><small>{n(effective[key],1)}% effective · {info.coverage_class==="cached_fundamentals_only"?"cached fundamentals only":`min ${info.min_sessions||"?"} sessions`} · {String(info.validation_status||"experimental").replaceAll("_"," ")}</small></div><label>Weight <input aria-label={`${labelFor(key)} weight`} type="number" min="0.1" max="1000" step="0.5" value={criteria[key]} onChange={e=>changeWeight(key,Number(e.target.value))}/></label><button aria-label={`Remove ${labelFor(key)}`} onClick={()=>remove(key)} disabled={activeKeys.length<=1}>×</button><details className="criterion-info"><summary>Why this factor?</summary><p>{info.description||"No description available."}</p><p><b>Hypothesis:</b> {info.hypothesis||"Not documented."}</p><p><b>Evidence state:</b> {info.evidence_basis||"No evidence note recorded."}</p></details></div>})}</div>
  <div className="formula-filters"><div className="filter-heading"><div><span className="label">Hard screens</span><p className="muted">Optional requirements applied before ranking. They add no points to the index.</p></div><div className="filter-add"><select aria-label="Add hard screen" value={addFilterKey} onChange={e=>setAddFilterKey(e.target.value)}><option value="">Add Filter…</option>{filterCatalog.map(f=><option key={f.key} value={f.key}>{f.label}</option>)}</select><button onClick={addFilter} disabled={!addFilterKey}>Add</button></div></div>{filters.length>0&&<div className="filter-list">{filters.map((f,i)=>{const info=filterInfo(f.field);return <div className="filter-rule" key={`${f.field}-${i}`}><b>{info.label}</b><select aria-label={`${info.label} operator`} value={f.operator} onChange={e=>updateFilter(i,{operator:e.target.value})}>{(info.operators||[]).map((op:string)=><option key={op} value={op}>{op}</option>)}</select><input aria-label={`${info.label} threshold`} type="number" step="0.1" value={f.value} onChange={e=>updateFilter(i,{value:Number(e.target.value)})}/><button aria-label={`Remove ${info.label} filter`} onClick={()=>removeFilter(i)}>×</button></div>})}</div>}</div>
  <div className="formula-presets"><div className="preset-list"><span className="label">Saved formulas</span>{presets.map(p=><div className={`preset-pill ${String(activePresetId)===String(p.id)?"active":""}`} key={String(p.id)}><button onClick={()=>usePreset(p)}>{p.name}</button>{!p.built_in&&<button className="preset-delete" aria-label={`Delete ${p.name}`} onClick={()=>del(p)}>×</button>}</div>)}</div><div className="save-formula"><input value={saveName} onChange={e=>{setSaveName(e.target.value);if(activePreset&&!activePreset.built_in)setDirty(true)}} placeholder="Formula name" aria-label="Formula name"/><button onClick={save} disabled={!saveName.trim()}>Save New</button>{activePreset&&!activePreset.built_in&&<button onClick={updatePreset} disabled={!dirty}>Update</button>}<button onClick={duplicatePreset}>Duplicate</button></div></div>
  {error&&<div className="v4-error"><span>{error}</span></div>}
  <div className="opportunity-index-head"><div><span className="label">Opportunity index</span><h2>Full-universe ranking</h2><p className="muted">Raw formula rank stays primary. Quality rank separately discounts stale, primary-only, unverified or incomplete inputs.</p></div><div className="mini-stats"><span>Eligible <b>{meta?.counts?.formula_complete??"—"}</b></span><span>Filtered <b>{meta?.counts?.filtered_out??0}</b></span><span>Returned <b>{rows.length}</b></span><span>Degraded <b>{meta?.quality_counts?.degraded??0}</b></span><span>Incomplete <b>{meta?.quality_counts?.incomplete??0}</b></span>{meta?.fundamental_cache?.used&&<span>Fund cache <b>{meta.fundamental_cache.cached_symbols??0}</b></span>}<span>Cache <b>{meta?.last_cache_update?new Date(meta.last_cache_update).toLocaleString():"—"}</b></span></div></div>
  <div className="metric-sort-bar formula-sort" role="group" aria-label="Click a metric to sort opportunity index">{[["score","Index"],["quality_adjusted_score","Quality-adjusted"],["quality_confidence","Confidence"],["williams","Williams %R"],["ma100_proximity","100MA distance"],["ma50_proximity","50MA distance"],["ma100_slope","100MA slope"],["approach_velocity","Approach"],["relative_volume","Rel volume"],["fundamental_quality","Fund quality"],["valuation_quality","Valuation"],["liquidity","Liquidity"],["symbol","Ticker"]].map(([key,label])=><button key={key} aria-pressed={sortKey===key} className={sortKey===key?"active":""} onClick={()=>sort(key as SortKey)}>{label}</button>)}<span className="muted">{sortDir==="desc"?"↓":"↑"}</span></div>
  {loading&&!rows.length?<p className="muted" role="status">Ranking cached market universe…</p>:<div className="opportunity-index-table"><div className="opportunity-index-row header"><b>Formula rank</b><b>Ticker</b><span>Index</span><span>W%R</span><span>100MA</span><span>Slope</span><span>Approach</span><span>Liquidity</span></div>{rows.map((r:any)=><div className={`opportunity-index-entry ${opportunityTone(r)}`} key={r.symbol}><button className="opportunity-index-row table-row-button" onClick={()=>setExpanded(expanded===r.symbol?null:r.symbol)} aria-expanded={expanded===r.symbol} title={`Open ${r.symbol} details`}><b>{r.formula_rank}<small>Q#{r.quality_adjusted_rank??"—"}</small></b><div><strong>{r.symbol}</strong><small>{r.sector||r.industry||"—"}</small></div><b>{n(r.score,1)}<small>Q {n(r.quality_adjusted_score,1)}</small></b><span>{n(r.raw_criteria?.williams,1)}</span><span>{r.raw_criteria?.ma100_proximity==null?"—":`${n(r.raw_criteria.ma100_proximity,1)}%`}</span><span>{r.raw_criteria?.ma100_slope==null?"—":`${n(r.raw_criteria.ma100_slope,2)}%`}</span><span>{n(r.raw_criteria?.approach_velocity,2)}</span><span>{money(r.average_dollar_volume_20d)}</span></button>{expanded===r.symbol&&<div className="opportunity-index-detail"><div className="source-state-grid"><span><i>Current sort position</i><b>{r.display_position}</b></span><span><i>Quality state</i><b>{String(r.data_quality?.state||"unknown").replaceAll("_"," ")}</b></span><span><i>Confidence</i><b>{r.quality_confidence==null?"—":`${n(Number(r.quality_confidence)*100,0)}%`}</b></span><span><i>Quality-adjusted rank</i><b>{r.quality_adjusted_rank==null?"—":`#${r.quality_adjusted_rank}`}</b></span><span><i>Quality-adjusted score</i><b>{n(r.quality_adjusted_score,1)}</b></span><span><i>Verification</i><b>{String(r.data_quality?.verification||r.verification_status||"unknown").replaceAll("_"," ")}</b></span><span><i>Price</i><b>{r.price==null?"—":`$${n(r.price,2)}`}</b></span><span><i>1D</i><b>{r.change_percent==null?"—":`${n(r.change_percent,2)}%`}</b></span><span><i>7D</i><b>{r.seven_day_percent==null?"—":`${n(r.seven_day_percent,2)}%`}</b></span><span><i>30D</i><b>{r.thirty_day_percent==null?"—":`${n(r.thirty_day_percent,2)}%`}</b></span><span><i>MA50</i><b>{n(r.ma50,2)}</b></span><span><i>MA100</i><b>{n(r.ma100,2)}</b></span><span><i>MA200</i><b>{n(r.ma200,2)}</b></span><span><i>Relative volume</i><b>{n(r.raw_criteria?.relative_volume,2)}x</b></span><span><i>Fundamental quality</i><b>{n(r.fundamental_assessment?.quality_score,1)}</b></span><span><i>Valuation quality</i><b>{n(r.fundamental_assessment?.valuation_score,1)}</b></span><span><i>Valuation anomaly</i><b>{String(r.fundamental_assessment?.anomaly||"not assessed").replaceAll("_"," ")}</b></span><span><i>Fundamental source</i><b>{r.fundamental_provider||"—"}</b></span></div>{r.fundamental_assessment?.flags?.length>0&&<details className="criterion-info"><summary>Fundamental flags · {r.fundamental_assessment.flags.length}</summary>{r.fundamental_assessment.flags.map((p:any,i:number)=><p key={`${p.code}-${i}`}><b>{String(p.code||"flag").replaceAll("_"," ")}:</b> {p.detail}</p>)}</details>}{r.data_quality?.problems?.length>0&&<details className="criterion-info"><summary>Data problems · {r.data_quality.problems.length}</summary>{r.data_quality.problems.map((p:any,i:number)=><p key={`${p.code}-${i}`}><b>{String(p.data_class||"data").replaceAll("_"," ")}:</b> {p.detail||p.code}</p>)}</details>}{r.data_quality?.sources?.length>0&&<details className="criterion-info"><summary>Sources & freshness</summary>{r.data_quality.sources.map((s:any,i:number)=><p key={`${s.provider}-${i}`}><b>{s.provider||"Unknown"}:</b> {s.age_minutes==null?"age unknown":`${n(s.age_minutes,1)} minutes old`}{s.url?<> · <a className="source-link" href={s.url} target="_blank" rel="noreferrer">source ↗</a></>:null}</p>)}</details>}<div className="criterion-breakdown">{activeKeys.map(key=><span key={key}>{labelFor(key)} <b>{n(r.criterion_scores?.[key],1)}</b> × {n(effective[key],1)}%</span>)}</div><div className="detail-actions"><button onClick={()=>onOpen(r.symbol)} title={`Open ${r.symbol} research`}>Open unified research</button>{r.source_url&&<a className="source-link" href={r.source_url} target="_blank" rel="noreferrer">Source ↗</a>}</div></div>}</div>)}</div>}
 </div>
}